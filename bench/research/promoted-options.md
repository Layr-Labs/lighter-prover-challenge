# Promoted optimization options

This is the checked-in optimization ledger for the Lighter Prover Challenge.
Update it from the live leaderboard and public submission notes before every
candidate is committed or submitted. Categories intentionally overlap; a
submission may contribute to more than one mechanism family.

- Snapshot time: 2026-08-13 12:41 America/Chicago
- Official runner: Apple M4 Pro Mac mini, 48 GB, five sequential 500-transaction fixtures
- Launch baseline: 3.18433342666123 tx/s
- Current record: 30.7380852237325 tx/s (`7477de7`)
- Current record speedup: 9.65x
- Promoted submissions: 145
- Solvers: 39

Refresh status: promotion #136 added delayed-reduction quadratic opening dot
products and #137 was a byte-identical redraw of that executable. The next 18
terminal submissions produced two failures and 16 rejections, spanning a broad
`24.9678–29.4376 tx/s` range. Promotion #138 (`4bfd557`, Yukon commit
`e268c13`) then sampled `29.9399105848455 tx/s`, a `0.0613407380081 tx/s`
improvement over #137. Its public note describes the first promoted appearance
of a scheduling/Metal/memory/FFT stack plus bitmap-guarded elimination of the
dense `PartitionWitness` zero-fill; the exact candidate was a redraw of an
earlier unpromoted v8 tree, not a byte-identical redraw of the prior frontier.
The next 25 terminal submissions produced three failures and 22 rejections;
the scored results ranged from `24.5588580629974` to `29.3561409924708 tx/s`
without a promotion. This campaign's exact-reuse plus initial-tree-retirement
submission (`9345ccf`, Yukon `89366c0`) was the upper result at
`29.3561409924708 tx/s`, `0.5837695923747 tx/s` below #138, and has been
reverted. The next six scored submissions (`d78bcde`, `5993547`, `a806b65`,
`9160b8d`, `6680dfc`, `b9453f1`) were all rejected; `d78bcde` was the closest
at `29.6774281689506 tx/s`. The next six submissions (`26c74c3`, `d9783b4`,
`a70ab61`, `0bd25aa`, `186ff6f`, `a355af7`) were also all rejected; their best
score was `28.9972739202195 tx/s`. The next seven submissions (`a18f3ad`,
`3753f20`, `95402b6`, `219cf2e`, `e8f40fb`, `2c546ec`, `f6c39e4`) produced
six rejections and one failure; the best scored result was
`29.4631471342932 tx/s`. `bfb5377` was then rejected at
`25.8294876495289 tx/s`. The next ten submissions (`b4b1ef5`, `c27fc32`,
`c8f0c8b`, `4d85fa7`, `fd479e7`, `7da6249`, `89ecf2a`, `bfdd47a`, `9c606e2`,
`dcd7fc8`) were all rejected; `9c606e2` was closest at
`29.4995031258566 tx/s`. The next six submissions (`79e7dce`, `3215720`,
`0d6b1dd`, `8e499cd`, `64dca84`, `3665729`) were also rejected; `64dca84` was
closest at `28.9596133369920 tx/s`. The next six submissions (`a8b640b`,
`215903c`, `c8cc3df`, `ac80930`, `ce32c2a`, `7d1a7ec`) were all rejected;
`ac80930` was closest at `26.1506237969623 tx/s`. Three newer submissions
(`7aa3ed9`, `5183775`, `0714a2e`) were followed by further rejections and
failures. Promotion #139 (`5a25029`, Yukon commit `a67126a`) then advanced the
record to `30.3111567697189 tx/s`. Its functional delta opens the light-proof
ramp from depth one to two, moves the exclusive-GPU claim until after the final
chunk-proof join, and stashes the final-block Metal store before rather than
after its page walk. The public note reports 61.7% ramp GPU utilization before
the change, versus 89.6% steady-state GPU utilization and 95.9% buffer-set
occupancy; it identifies the `range_u32_quotient` kernel's 14.1x wire-LDE
reread as the next measured serialized-station target. Promotion #140
(`7b2d3a6`, Yukon commit `5b329f4`) then redrew that executable at
`30.4408404924460 tx/s`; its only source changes are two comment markers, and
its public note explicitly labels the fire marker-only with no mechanism
change. Promotion #141 (`cdee956`, Yukon commit `0a470b3`) then redrew the same
functional executable at `30.4758937588950 tx/s`; its exact diff again changes
only those two comment markers, and its public note says “Markers only” and
“No mechanism change this fire.” Promotion #142 (`c90e02f`, Yukon commit
`814fe26`) was another marker-only redraw at `30.5343213569663 tx/s`.
Promotion #143 (`9a57cdd`, Yukon commit `612ea7a`) advanced the record to
`30.6448190741910 tx/s` with three mechanisms relative to the frontier:
strength-reduced/two-way packed 67-bit exponentiation evaluation, deferred
alpha-weighted reduction in the Metal Range/U32 quotient kernel, and corrected
chain-spine backlog accounting. Promotion #144 (`81fcf73`, Yukon commit
`5287cfe`) redrew that functional executable at `30.6618694127846 tx/s` with
only inert marker changes. Promotion #145 (`a955674`, Yukon commit `7477de7`)
then advanced the record to `30.7380852237325 tx/s` by replacing the small
AIR-only `poseidon2.metallib` with a fat artifact containing an M4-compatible
`applegpu` slice; its public component measurement reduced cold library plus
ten-pipeline creation from about `240.2 ms` to `1.5 ms`. At the terminal
refresh, no post-#145 promotion had landed and `origin/master` remained exactly
`7477de7`. Direct transcript allocation deletion (`887e5b0`) was rejected at
`30.4508863812249 tx/s`; exclusive-phase spin completion waits were rejected
at `30.4206092764959` (`bb20477`) and `30.3974360350032 tx/s` (`1b8ad6d`).
Eight newer submissions were validating.

## Category totals after the latest append

The prior fully categorized snapshot contained 121 promoted submissions. The
table below applies the 24-entry append ledger in the next section. Shares use
145 as the denominator and do not sum to 100% because categories overlap.

| Optimization category | Successful lineage hits | Share of 145 |
|---|---:|---:|
| Gates, constraints, quotient evaluation | 42 | 29.0% |
| Metal/GPU/shaders | 42 | 29.0% |
| Memory, buffers, copies | 27 | 18.6% |
| Scheduling and parallelism | 22 | 15.2% |
| Permutation and sigma | 16 | 11.0% |
| Witness generation/generators | 15 | 10.3% |
| FFT/LDE | 12 | 8.3% |
| FRI/openings/Merkle queries | 13 | 9.0% |
| Startup, embedded circuits, codecs | 13 | 9.0% |
| Challenger/Fiat-Shamir transcript | 2 | 1.4% |
| Allocators | 1 | 0.7% |
| I/O, mmap, zero-copy | 0 | 0.0% |
| Proof encoding/serialization | 0 | 0.0% |

Supplemental non-mechanism tag: ten of the 24 appended promotions were
marker-only redraws of an existing executable. They are retained below because
they are real leaderboard promotions but add no optimization-category hit.

## Append ledger: promotions 122-145

Rows are newest first, matching the live leaderboard. `Delta kind` is based on
the submitted diff and public note, not the score alone. Inherited mechanisms
are not re-counted for a marker-only redraw.

| Overall promotion | Score (tx/s) | Commit | Solver | Delta kind | Public-note title | Categories added |
|---:|---:|---|---|---|---|---|
| 145 | 30.7380852237325 | `7477de7` | jungjipdo | functional artifact | Ship a fat `poseidon2.metallib` so each worker loads pre-lowered GPU code instead of lowering it | Metal; Startup |
| 144 | 30.6618694127846 | `5287cfe` | AlexLaevski | redraw | Pure tip multi-draw for #1 (pepedesigner playbook) | none |
| 143 | 30.6448190741910 | `612ea7a` | exakoss | functional stack, redraw of an unpromoted composition | Rebase the identical composed executable onto the latest 30.5343-TPS frontier | Gates; Metal; Scheduling |
| 142 | 30.5343213569663 | `814fe26` | AlexLaevski | redraw | Pure tip multi-draw for #1 (pepedesigner playbook) | none |
| 141 | 30.4758937588950 | `0a470b3` | AlexLaevski | redraw | Pure tip multi-draw for #1 (pepedesigner playbook) | none |
| 140 | 30.4408404924460 | `5b329f4` | AlexLaevski | redraw | Pure tip multi-draw for #1 (pepedesigner playbook) | none |
| 139 | 30.3111567697189 | `a67126a` | FatihSolak | functional scheduling and prewarm correction | v11: ramp depth 2 — first lever from the measured road-to-50 idle decomposition | Metal; Memory; Scheduling; Startup |
| 138 | 29.9399105848455 | `e268c13` | FatihSolak | functional stack, redraw of unpromoted v8 tree | Profile-driven scheduling stack v8 re-roll (draw sampling; tree unchanged) | Metal; Memory; Scheduling; Witness; FFT; Startup |
| 137 | 29.8785698468374 | `59c0155` | jungjipdo | redraw | Redraw 1 of the 29.8001 frontier (marker 701-claude-fable-r2) | none |
| 136 | 29.8000549369782 | `ae44516` | exakoss | functional | Delayed-reduction quadratic opening dot products | FRI/openings |
| 135 | 29.7608320281085 | `1ad5dd0` | AlexLaevski | redraw | Pure tip multi-draw for #1 (pepedesigner playbook) | none |
| 134 | 29.3026242666808 | `acc6896` | jungjipdo | functional | Keep six-bit random access off the shared Metal quotient queue | Gates; Metal |
| 133 | 28.9950746592419 | `b7a4e59` | XieLeiaaa | redraw | Redraw of the new frontier 39d7a81 (marker 324) | none |
| 132 | 28.8556707796600 | `39d7a81` | jungjipdo | functional stack | Column-store buffer pool: stop re-faulting 720 MiB of Metal pages every chain step | Metal; Memory; FRI; Allocators |
| 131 | 28.7909877101153 | `2f2951e` | bot66 | redraw | Round 49: 24d9690a redraw (marker 293) | none |
| 130 | 28.6874019292879 | `24d9690` | Gajesh2007 | functional | Offload the full no-lookup permutation product chain to Metal | Gates; Metal; Permutation |
| 129 | 27.4921224081074 | `dd2f8e9` | pepedesigner | redraw | Redraw 300 of the 27.23 frontier | none |
| 128 | 27.2303124960222 | `e283729` | exakoss | functional stack | Three CPU work deletions plus a 2/2-winning light-proof window | Gates; Memory; Scheduling; Permutation |
| 127 | 27.1893008823957 | `455c48f` | rawqubit | functional stack | p90 stack: occupancy + restores + always-detach + quotient occupancy fix + parse/load | Metal; Memory; Scheduling; Startup |
| 126 | 27.0925037754165 | `e11fedd` | jungjipdo | functional | Packed dense fusion for the final Interleave/Uninterleave CPU pair | Gates; Memory |
| 125 | 26.9753651803152 | `c1a8200` | rawqubit | functional stack | p90 stack: occupancy + restores + always-detach + quotient occupancy fix + parse/load | Metal; Memory; Scheduling; Startup |
| 124 | 26.9532156206450 | `50bed64` | beibei030 | redraw | Round 46 - Draw on the Poseidon2-linear-layer frontier | none |
| 123 | 26.9017907110000 | `b30a4ba` | exakoss | functional stack | Direct generator CSR, coefficient-slot FRI reduction, and scalar CPU Poseidon2 diagonal | Memory; Witness; FRI |
| 122 | 26.8995828739518 | `9f71f64` | i34-9 | functional stack | A narrower operand split and a different addition chain in the hash kernels | Gates; Metal; Memory |

## Interpretation for the next search

The latest functional evidence says the shared Metal queue is saturated under
the official active-witness workload. Two consecutive frontier improvements came
from *removing* latency-heavy gate shapes from the shared quotient command:
67-bit exponentiation first, then six-bit random access. The best near-term
search is therefore per-shape CPU/GPU admission control, not indiscriminate
offload expansion.

The second promising seam is allocation and first-touch behavior. The first
allocator-category hit restored sequential-worker jemalloc decay, while the
column-store pool stopped repeatedly faulting roughly 720 MiB of shared Metal
pages per chain step. Buffer lifetime, exact-size reuse, detach policy, and
cross-proof resident ownership should be evaluated under the actual sequential
one-worker-at-a-time harness, while remembering that each process has a 48 GB
machine available to itself.

The newest functional promotion shows that opening evaluation still contains
profitable base/extension field arithmetic: accumulating each quadratic limb
in 160 bits and reducing once per polynomial deleted repeated Goldilocks
reductions without changing opening values. Treat opening dot products and
other exact delayed-reduction seams as live, but do not count the following
marker-only redraw as a second mechanism hit.

Promotion #138 adds a distinct low-level lesson: the dense partition-witness
constructor was paying a serial zero-fill before setting only the values guarded
by its bitmap. Eliminating those stores while making the one formerly
unguarded materialization path return zero for unset slots is value-exact and
attacks both serial witness time and DRAM bandwidth. Its larger inherited stack
also reinforces portable unconditional QoS/window/prefault/readback/FFT changes,
but the note explicitly warns that queue-depth-triggered routing regressed on
the official host. Treat the zero-fill deletion as the clearest new mechanism;
do not infer that every state-conditional scheduler in the stack transferred.

Do not treat marker-only redraws as algorithmic evidence. They show that the
official score has enough variance for a strong unchanged tree to promote, so
any small candidate should be screened against repeated frontier-exact control
draws and should not be kept on a single favorable result.

## Local hypothesis outcomes

These rows are negative or provisional local evidence, not promoted-category
hits. Keep them here so later candidates do not repeat a disproven admission
decision and so M1/M4 transfer assumptions remain reviewable.

| Date | Hypothesis | Reproducible evidence | Outcome |
|---|---|---|---|
| 2026-08-13 | Use idle Metal capacity for CPU-dominant LDE work, first by comparing production-shape CPU coset-LDE with the existing GPU NTT path | Alternating five-sample medians at 136 columns: degree 14 CPU `34.33 ms` versus GPU NTT+Merkle `46.10 ms` and estimated NTT-only `43.16 ms` (`0.795x` CPU/GPU ratio); degree 16 CPU `153.94 ms` versus GPU NTT+Merkle `180.36 ms` and estimated NTT-only `168.62 ms` (`0.913x`). A modeled concurrent degree-16 column split is `1.45x` at 49% available GPU capacity, but unified-memory contention and phase alignment are unmeasured. | **Reject straight reassignment; keep heterogeneous split as conditional research.** Align GPU busy intervals with CPU LDE phases before writing a scheduler. This is distinct from the already-rejected whole-commitment GPU route only if CPU and GPU process disjoint columns concurrently while preserving early commitment overlap. |
| 2026-08-13 | Interpret whole-worker CPU sampling as a station/criticality map before choosing another optimization, and test whether the single shared Metal buffer set is starving CPU work | A 28-second, 1 ms release-worker sample produced 409,039 leaf samples. Compute averaged `6.03` cores; FFT/NTT was `41.5%` of compute, Poseidon2 CPU hashing `25.5%`, gates `14.6%`, and memory/copies `5.4%`. Wait attribution was main-thread worker joins `46.2%`, parked Rayon workers `38.5%`, unattributed `13.8%`, Rayon join/steal `1.4%`, and Metal buffer-set acquisition only 26 samples (`0.006%` of all samples). | **Keep the diagnostic; reject buffer-set widening as the next M1 angle.** The original “58.7% waiting” headline counted threads rather than cores and is retracted. Profile FFT by caller/shape/phase before changing algorithms; do not infer criticality from leaf cost alone or repeat blanket GPU NTT routing. |
| 2026-08-13 | Collapse the small Merkle parent tail after an isolated harness estimated a `~0.41%` optimistic worker ceiling | The production `diagnostic_profile` trace captured 644 Metal command buffers. GPU busy union was `18.258 s`; summed command durations were `27.154 s` over a `35.962 s` span (`1.487x` overlap). Seven tail levels across 274 `merkle_tree` buffers account for only `~0.199 s` of GPU time before the overlap discount; Merkle work was `15.286 s` summed. | **Reject before implementation.** Other command buffers already fill much of the per-tree barrier floor, and the production ceiling is smaller than the isolated-tree estimate. Do not implement or tune a tail-cascade kernel. |
| 2026-08-12 | Expand the `RandomAccessGate<bits=6>` stack scratch so the only CPU-surviving shape avoids one heap allocation/free per 32-row evaluator call | The production census leaves bits=3/4 on Metal and bits=6 on CPU, so the old 256-slot stack fast path missed the required 1,024 slots on roughly 852,000 calls. A test-only forced-heap same-binary release microbenchmark measured stack `140 ns/row` versus heap `142 ns/row`, nominal `1.0143x`, with overlapping arms. The `2 ns/row` delta is only `~0.055 s` aggregate CPU and an optimistic `~7 ms / 0.02%` wall ceiling. | **Reject before full proving and revert.** `Vec<MaybeUninit<F>>` does not initialize its 8 KiB logical payload, so the change removed only a jemalloc tcache allocation/free pair. Preserve the generic heap path and require an arithmetic/materialization deletion before revisiting this gate. |
| 2026-08-12 | Pack the CPU-surviving `RandomAccessGate<bits=6>` 63-selection fold across the fixed 32-row quotient batch, traverse selector vectors first, and fuse each `x + b * (y - x)` through AArch64 multiply-accumulate | Two rotated same-binary confirmations measured scalar `130 ns/row`, packed `113–114 ns/row`, and packed/fused `107 ns/row`; all arms were disjoint and all four RandomAccess tests passed. Release SHA-256 `7de015f21f64a08b1eff76c9632887741222c7bbbf0d228134d0e346059befb0`; protected verification 5/5. `B-C-C-B`: control `29.598131208,30.218835167 s`; candidate `28.582630792,30.876317000 s`; means nominally favored candidate by `0.598523%` runtime, but pairings split `-3.430961%/+2.175735%` under `2.294 s` candidate drift. | **Reject by pairing rule and revert without submission (`dec9697`).** The evaluator improvement is real, but its `~78 ms` projected wall effect cannot explain multi-second protected variance. Preserve implementation commit `941f7e1` for M4/direct measurement; do not treat the favorable mean as throughput evidence. No Metal source or metallib changed. |
| 2026-08-11 | Parallelize only the recursive-proof-triggered chain generator round during the last path's proven exclusive drain | The saved trace showed all 30 late successors had already prepared predecessor-independent witness work and waited `2.898–9.940 s`; the remaining join-end to prove-start dependency totaled `234.631 ms` (`7.821 ms/step`). `PLONKY2_PARALLEL_CHAIN_FEED=0` restored serial worklists. Cargo check passed; release SHA-256 `d2ecb6e7dc381d4e3f08dea145c5d75160ab20bd044aff051efa0571b3d157ae`; trusted verification 5/5. Protected `B-C-C-B`: serial `32.425009041,29.618962542 s`; candidate `29.765828500,29.694780875 s`; pairings `-8.20102%/+0.255979%`. | **Reject by pairing rule and revert without submission.** The 2.806 s control drift exceeds the 234.6 ms ceiling and dominates the favorable mean; the quiet reverse pairing is negative. The existing successor threads already form an implicit ready queue, so do not build another. A terminal refresh attempt was unavailable; retain the successful 21:16 #141 snapshot. |
| 2026-08-11 | Give an urgent degree-14 chain proof priority for one adjacent Poseidon/Range-U32/permutation quotient submission burst, using the existing deterministic `SPINE_BACKLOG >= 3` signal | A diagnostic release (SHA-256 `15e035f7fbeebcecc5bba3b6e566f4250a02217bb684c431f651175df8b48ed6`) emitted 10,196 events. The light transaction queue retired at `22.715 s` with the chain only through step 18, leaving 30 serial proofs and `3.649 s` of tail. Candidate SHA-256 `fc9df0d96b9d3e11c5c43e5b87df9c74cee8f874fa26bd227aadfdc9d87cf945`; Cargo check and candidate-default trusted verification passed. Direct same-binary `B-C-C-B`: control `30.17,29.89 s` (mean `30.030`), candidate `27.86,32.61 s` (mean `30.235`), candidate `+0.682651%` runtime / `-0.678022%` throughput; pairings `-7.656613%/+9.100033%`. | **Reject and revert without submission.** The new host-side burst lock did not advance the serial chain repeatably and slightly regressed aggregate runtime. Preserve the trace evidence, but do not add another global queue-order lock; next target must advance predecessor-independent chain work or remove a concrete dependency. Terminal refresh retained marker-only #141; four submissions were validating. |
| 2026-08-11 | Preserve the first eight-column fill/absorb latency exactly, then fuse only later wire-commit groups in pairs so two Poseidon2 permutations share private state without delaying the first GPU command | `PLONKY2_WIRE_ABSORB_TAIL_PAIR=0` restored the exact promoted loop. Cargo check and candidate/control 17-column full-tree/cap/path differentials passed; release SHA-256 `11a2c41ef4c2fd6030225ae4a40975f5ed7317ec708111e00aad8dbe64788306`; candidate gate plus first B-C pairing passed trusted verification 3/3. Scalar B1 was `30.949020167 s`; fused-tail C1 was `31.455793292 s`, candidate `+1.637445%` runtime / `-1.611065%` throughput. | **Reject after the first pairing and revert without submission.** One lost pairing makes the declared two-pair rule impossible, so C2/B2 were not run. The prior all-paired split was not caused solely by first-absorb delay; close this fusion line on M1 unless direct M4 command/state counters support a different design. Frontier remained marker-only #141 at `30.4758937588950 tx/s`; seven submissions were validating. |
| 2026-08-11 | Stage 32 Range/U32 quotient rows by the common first 120 wire columns in 30 KiB of Metal threadgroup memory, so all 32 SIMD lanes evaluate a row and only the 16-column tail remains device-resident | A production census found 105 Range/U32 jobs and broad 114–136-column records, ruling out compact-family partitioning. `PLONKY2_RANGE_U32_TILE=0` restored the scalar entry point. Cargo check; RangeCheck, U32, byte/quintic and combined proof differentials in both modes; and trusted verification 5/5 passed. Release SHA-256 `8d6dbb73aa8b02b235726d43a3ec33910b4b080b14087fa6b163ae7e88b4a1c6`. `B-C-C-B`: control `38.890966250,38.572955916 s` (mean `38.731961083`); tile `38.428841041,39.102838292 s` (mean `38.765839666`), candidate `+0.087469%` runtime / `-0.087393%` throughput; pairings `-1.188258%/+1.373715%`. | **Reject by pairing rule and revert without submission.** The SIMD-native layout removed the 16-row predecessor's half-idle lanes but remained neutral-negative on M1. Do not sweep prefix widths/rows locally; require M4 occupancy, cache-hit, and queue-tail counters before revisiting. Terminal refresh found marker-only promotion #141 at `30.4758937588950 tx/s`. |
| 2026-08-11 | Compute the fused production permutation path's two independent post-inversion Z accumulations with one Rayon join instead of two serial calls | Built on #140-equivalent code with `PLONKY2_PARALLEL_Z_CHAINS=0` restoring serial construction. Cargo check and the production-shape fixed-mask symbolic/raw-limb test passed in candidate and control modes; an older broader test independently fails in both modes on its invalid five-point non-power-of-two case and was not modified. Release SHA-256 `a149fbe83bce6d851d464d3af2add242187de1d358d478ebaa03a09ac81605b2`; candidate-default plus fixed `B-C-C-B` trusted verifier 5/5. Control `30.587060542,31.190676041 s` (mean `30.888868292`); candidate `31.385469750,29.611569541 s` (mean `30.498519646`), nominal `-1.263719%` runtime / `+1.279894%` throughput, but pairings split `+2.610284%/-5.062752%`. | **Reject by pairing rule and revert without submission.** The first pairing loses decisively relative to the 10–18 ms expected ceiling, while candidate samples drift 1.774 s. Global-pool scheduling noise dominates this tiny serial tail; keep the serial `vec!` unless a dedicated phase/pool or direct span counters show uncontended critical work. |
| 2026-08-11 | Fill up to 16 retained wire-LDE columns in one Rayon batch and fuse two sequential rate-eight Poseidon2 absorbs so the 12-lane per-leaf state stays live in the Metal kernel | Built on promotion #139 with `PLONKY2_WIRE_ABSORB_FUSION=0` restoring the exact eight-column streamed path. Cargo check and both candidate/control 17-column Metal full-tree/cap/path differentials passed; release SHA-256 `7fc550d1bab4b3aa1263803b9d7ded8cd145b88370da9ec850607fda3241067b`; candidate-default plus fixed `B-C-C-B` trusted verifier 5/5. Control `35.424763792,29.965593667 s` (mean `32.695178730`); candidate `30.936116792,30.167929875 s` (mean `30.552023334`), nominal `-6.554958%` runtime / `+7.014774%` throughput, but pairings split `-12.670930%/+0.675228%` while control endpoints drifted by 5.459 s. | **Reject by pairing rule and revert without submission.** The tight candidate samples do not rescue the reversed warm pairing; the favorable aggregate is dominated by cold/cache/order drift. Keep eight-column overlap granularity. A future revisit needs direct M4 command/state-traffic counters or a different decoupling that does not delay the first absorb. |
| 2026-08-11 | Extend #139's final wire-store prewarm to the same commitment's exact 272 MiB coefficient input and ~128 MiB Merkle output, fully touching them before publishing exact-size-only stashes | `PLONKY2_FINAL_SET_PREWARM=0` preserved the promoted column-store-only behavior. Cargo check and the exact-size guard test passed; release SHA-256 `124031eb1719fb1094994e6a9a93f83b217b99d0a4780a47bce3767b3d18d98c`; trusted verifier 5/5. Fixed `B-C-C-B`: control `36.780856334,40.475705500 s` (mean `38.628280917`); candidate `42.431655375,39.706284625 s` (mean `41.068970000`), `+6.318399%` runtime / `-5.942903%` throughput. Pairings split `+15.363424%/-1.900945%`. | **Reject and revert without submission.** Moving another 400 MiB of first-touch work under the light pipeline increases unified-memory and VM contention more than it shortens the final tail. Do not extend prewarming or tune page stride/QoS without direct M4 counters. |
| 2026-08-11 | Stage 16 quotient rows by 136 wire-LDE columns in Metal threadgroup memory so the combined Range/U32 gate families reuse each field element instead of rereading it from device memory | Built on promotion #139 (`a67126a`) with exact scalar control selected by `PLONKY2_RANGE_U32_TILE=0`. Cargo check and five focused Metal/CPU quotient differentials passed; release SHA-256 `351e738e5fd2fe356924ed8e613c9b3a0682b107d419e0bca5246fea7672bfcb`; trusted verifier 5/5. Fixed `B-C-C-B`: control `47.109057000,44.662899292 s` (mean `45.885978146`); tiled `46.737068125,46.334087584 s` (mean `46.535577855`), `+1.415682%` runtime / `-1.395921%` throughput. Pairings split: `-0.789633%` then `+3.741782%`. Both arms paid identical cold source compilation because the embedded metallib was intentionally not regenerated before the performance gate. | **Reject by pairing rule and revert without submission.** The M1 result does not validate the promoted note's M4 projection, and the 32-loader/16-evaluator mapping may underutilize a 32-lane SIMD group. Revisit only with direct M4 kernel counters and a SIMD-native layout; do not run blind M1 tile-size permutations. |
| 2026-08-11 | Retune the fixed light-transaction proof window from six to five or seven, using the existing runtime selector, to trade `tx_proof_window_join` stalls against CPU/GPU contention and allocation pressure | Existing diagnostic trace showed 47 window joins totaling `18.464573 s` with a `1.750145 s` maximum. Clean-source release SHA-256 `3d7036f089a134e0f78ce304e4e3554a82c7f0e7d3c5fa9a039809e698bf1f6e`; fixed symmetric order `6-7-5-5-7-6` passed trusted verification 6/6. Depth 6 `31.639909583,30.418927417 s` (mean `31.029418500`); depth 5 `32.849648000,31.041157875 s` (mean `31.945402937`, `+2.951987%` runtime, both pairings lost); depth 7 `34.440152416,29.951259625 s` (mean `32.195706020`, `+3.758651%`, pairings split `+8.850350%/-1.537424%`). Yukon note `f36ea396-3c8e-495a-8eb0-943964650119`. | **Reject both neighbors; retain depth six with no source change or submission.** Five underfills the pipeline and loses consistently; seven increases contention/allocator churn enough to regress aggregate despite one favorable endpoint. Adaptive scheduling needs a phase signal, not a different static integer. |
| 2026-08-11 | Unroll the bitmap-guarded full-witness gather four cells at a time to expose independent representative-map, bitmap, and value-load chains to Apple out-of-order execution | `PLONKY2_WITNESS_GATHER_UNROLL=0` restored the exact #138 iterator loop in one release executable. Cargo check and both required `PendingPartitionWitness` tests passed; release SHA-256 `d42a8d12dbb6aaa3cfcf755a0ce1b126b7b36998f2625a97344d5ae4fa96f924`; candidate-default protected gate and fixed `B-C-C-B` passed trusted verification 5/5. Control `32.142156083,29.839181750 s` (mean `30.990668917`); candidate `30.457280084,30.711325958 s` (mean `30.584303021`), nominal `-1.311252%` runtime / `+1.328675%` throughput-equivalent, but pairings split sharply at `-5.241951%` and `+2.922815%`. Yukon note `95ec0035-2ae1-4ea0-b028-ebcfee26fa57`. | **Reject by the predeclared pairing rule; revert without submission.** The candidate samples were tight, but the control endpoints moved by 2.303 s and reversed the second pairing, so the favorable mean is not attributable to unrolling. Preserve the compiler-generated iterator loop; do not test wider unrolls without assembly or hardware-counter evidence. |
| 2026-08-11 | Isolate demand-zero `PartitionWitness` allocation from bitmap removal: use true zeroed pages only for `GoldilocksField`, retain #138's bitmap-guarded full-witness gather, and restore exact #138 allocation with `PLONKY2_ZEROED_WITNESS_ALLOC=0` | One diagnostic trace per mode first showed aggregate gather work of `3.469742 s` for #138, `2.857983 s` for zeroed+guarded, and `4.222018 s` for zeroed+unguarded; the latter had a `494.060 ms` maximum gather, consistent with harmful unset-page first touches but not sufficient as timing evidence. The refined candidate used a `TypeId` gate so all non-Goldilocks fields retained `vec![F::ZERO]`; Cargo check and both required `PendingPartitionWitness` tests passed. Release SHA-256 `ca4e6719c9da2115ea4a7249e40589568a723ba5b07b66f0caa62aaecaab9e59`; candidate-default protected gate passed, then fixed `B-C-C-B` passed trusted verification 4/4: control `28.929890750,30.249941542 s` (mean `29.589916146`); candidate `29.725723083,30.664303375 s` (mean `30.195013229`), `+2.044944%` runtime / `-2.003964%` throughput-equivalent, with both pairings lost by `2.750900%` and `1.369794%`. Yukon note `7f0ca8ad-82af-4649-8abf-946b3e33d33b`. | **Reject and revert without submission.** The isolated allocator arm is decisively negative; true zeroing/first-touch work costs more than #138's uninitialized allocation even when the bitmap prevents unset-slot reads. Preserve #138 exactly and close this demand-zero line unless M4 hardware counters show materially different VM behavior. |
| 2026-08-11 | Allocate `PartitionWitness::values` with a true zeroed allocator when `F::ZERO` is the all-zero representation, allowing an unguarded full-witness gather without a Rust zero-fill loop | Generic byte-representation guard falls back to initialized values for non-all-zero fields; Goldilocks takes `alloc_zeroed`. Cargo check and candidate recursive proof differential passed; release SHA-256 `821bcc2530c12a02bcbc81300f88c393540b957c675bb52ed4c5c36c92b241b0`; trusted verification 4/4. Fixed `B-C-C-B`: control `30.782584500,30.130072208 s` (mean `30.456328354`); candidate `29.266747375,31.104730041 s` (mean `30.185738708`), nominal `-0.888451%` runtime / `+0.896416%` throughput-equivalent, but pairings split with `-4.924%` then `+3.235%`. Yukon note `2465c2ef-572a-42c3-aa49-c8203860a2ad`. | **Reject by the predeclared pairing rule; revert without submission.** The positive mean is not robust enough for keep because allocator/page behavior produced extreme opposite pairing signs. Profile allocation and first-touch per shape, then separately test zeroed allocation with guarded versus unguarded gather before reconsidering. |
| 2026-08-11 | Eliminate dynamic slice bounds checks from every full-witness output cell by using the circuit's validated representative-map and disjoint-segment invariants, while preserving #138's bitmap zero guard | Same-binary `PLONKY2_UNCHECKED_WITNESS_GATHER`; an initial debug assertion revealed a valid virtual-target suffix (`138243` total map entries versus `138240` wire entries) and was corrected before unsafe execution. Cargo check and the candidate recursive witness/proof differential passed; release SHA-256 `75aa7b436e8d9062e234112332787a52291a59c53168256d3516b2e918e8693d`; trusted verification 4/4. Fixed `B-C-C-B`: control `30.242940250,29.607084708 s` (mean `29.925012479`); candidate `30.453952500,29.767246833 s` (mean `30.110599667`), `+0.620174%` runtime / `-0.616352%` throughput-equivalent; both pairings lost. Yukon note `3f007e51-7780-46a4-b2fc-a99e7f83a805`. | **Reject and revert without submission.** The frontier running-index loop is already optimized well enough, while the explicit unchecked form changes address arithmetic and adds no positive signal. Do not add unsafe indexing without release disassembly or counters demonstrating a surviving bounds check. |
| 2026-08-11 | Fill only unset `PartitionWitness` representative slots with zero at `full_witness` entry, then replace per-output-cell bitmap selection with an unguarded values gather | Same-binary selector `PLONKY2_LATE_ZERO_WITNESS`; Cargo check passed; release SHA-256 `4de3e0720759bc60cbd5e5641bfdd6759e3959045935dfe076527e09fd6c6595`; one focused recursive witness equivalence passed. A second pre-materialization test exposed an invalid #138-era raw-uninitialized-slot oracle and was quarantined; trusted proofs passed 4/4. Fixed `B-C-C-B`: control `30.014395583,30.033468791 s` (mean `30.023932187`); candidate `30.407384250,31.086269542 s` (mean `30.746826896`), `+2.407728%` runtime / `-2.351120%` throughput-equivalent; both pairings lost. Yukon note `0741afd8-fd14-41a3-be72-0192941e0c6e`. | **Reject and revert without submission.** Compact bitmap tests are cheaper than a serial sparse fill plus millions of write-allocate stores immediately before the parallel gather. Preserve #138's guarded materialization and do not retest wider/parallel zero fill without direct evidence that the bitmap selection is critical. |
| 2026-08-11 | Halve `PartitionWitness::full_witness` row-task fanout from 16 to eight for degree-14/16 pipeline proofs while retaining 16 for the degree-18 final proof, reducing global Rayon contention under the six-proof window | Diagnostic SHA-256 `40374201c823dd8ad084df4a9f31af8b6f17f4d7a4b22e3c03731eac40b81af8`: 106 materializations, 617.2M output cells, set density `41.5–50.9%`; degree-16 heavy proofs averaged `16.2 ms`, but light proofs averaged `164.7 ms` with a `1.286 s` maximum. After quarantining one stacked/non-isolated screen, a clean incremental release on `82635d8` had SHA-256 `3e3a990ea8fbd9e5eb745e6a45ee9f7938d0732a0bd0f452397bd856f287482b`. Fixed `16-8-8-16`: control `29.272534541,29.082638459 s` (mean `29.177586500`); eight `30.294703875,28.723772166 s` (mean `29.509238020`, `+1.1367%`); pairings split 1-1; trusted verification passed 4/4. | **Reject and revert without confirmation or submission.** The tail is real, but it reflects competition for workers rather than task-creation overhead; eight larger segments sacrifice useful parallelism and do not reliably resolve starvation. Keep 16 and do not blindly test four/one. |
| 2026-08-11 | Combine exact-size reuse for recurring Metal column stores below 256 MiB with ownership transfer of the three ephemeral initial FRI Merkle trees, materializing their challenged leaves/paths first and returning their stores to the pool before fold-tree query construction | Starts from promotion #138 (`e268c13`) plus research ledger `581bac9`; focused exact-pool test and Cargo check passed. Same release SHA-256 `8b6b4c6ed8a3982fa818e2c314a587753a593f4ca784c10d72f3db67caab1d5a`; candidate-default cold gate and fixed `B-C-C-B` all passed trusted verification (5/5). B restored best-fit plus retained trees: `37.153416166,37.027430625 s`; C enabled exact small-bin reuse plus early tree retirement: `36.090781292,35.907613125 s`. Candidate mean `35.999197209 s` versus control `37.090423396 s`, `2.94%` lower runtime (`~3.03%` throughput-equivalent), and both mirrored pairings won. Official submission `9345ccf4-7264-4f4b-ab72-9bcb6755ec1e` (Yukon `89366c0`) scored `29.3561409924708 tx/s`, `-0.5837695923747` versus #138. | **Officially rejected and reverted in `029af57`.** The result landed in the fast band, so the combined local M1 signal did not transfer to the M4 Pro. Because exact-bin reuse and tree retirement were toggled together, the official result does not isolate either component; do not keep or resubmit the stack without new M4-side evidence. |
| 2026-08-11 | Retune the promoted FRI coefficient-slot Rayon block from 2,048 to 1,024 or 4,096 to trade task granularity against Apple-Silicon cache locality | Same-binary selector in both generic and Goldilocks extension-degree-two reducers; `cargo check` and release build passed; release SHA-256 `3f12ff6e3da450d8cd85de2048667f153bccabf477b54dec1df6c0b40727b650`; trusted verification passed 6/6. Symmetric order `2048-4096-1024-1024-4096-2048` produced 2,048 `29.671916625,45.165871291 s` (mean `37.418893958`), 4,096 `44.087350875,41.906574208 s` (mean `42.996962541`, `+14.9071%`), and 1,024 `37.227142208,55.531989084 s` (mean `46.379565646`, `+23.9469%`). Endpoint drift was large, so deltas are directional rather than precise; neither alternative had a positive aggregate signal. | **Reject and revert without confirmation or submission.** Retain 2,048. Smaller blocks add scheduling/full-polynomial traversal overhead; larger blocks reduce useful parallelism or exceed the favorable working set. Revisit only with per-shape counters or direct M4 access, not another blind M1 power-of-two sweep. |
| 2026-08-11 | Drop per-proof wire, Z/partial-product, and quotient coefficient vectors immediately after the reduced FRI opening polynomial is constructed, retaining their Merkle trees/caps and the reusable constants/sigmas oracle | Ownership refactor kept oracle zero intact and cleared only the three ephemeral `PolynomialBatch::polynomials` vectors at their exact last read; `PLONKY2_RETAIN_EPHEMERAL_COEFFICIENTS=1` restored frontier lifetime in the same binary. Cargo check passed; release SHA-256 `4891dc4dea1cf362a427d96cc2ececfb58cecdb01a9a29fa6c6fca16f0cd5923`; trusted verification passed every run (5/5 overall, 3/3 candidate). Fixed `B-C-C-B`: control `29.915660042,30.319586584 s`; candidate `30.211574167,30.778590958 s`. Control mean `30.117623313 s`, candidate `30.495082562 s`: candidate `1.2533%` slower and both pairings lost. | **Reject and revert without confirmation or submission.** The last-use proof is sound, but eagerly dropping hundreds of inner allocations serializes allocator/destructor work directly before the FRI FFT/query phase; that cost exceeds any reduced memory pressure. Keep the current scope-end destruction and pursue buffer reuse/move ownership rather than eager freeing. |
| 2026-08-11 | Stripe each limb of the newly promoted Goldilocks quadratic opening dot product across four independent 160-bit accumulators, then merge before the same two reductions, to expose multiply/carry instruction-level parallelism on Apple cores | The candidate preserved arbitrary zip lengths and the existing `u32::MAX` chunk bound; focused differentials passed all zero, unequal-length, boundary, noncanonical, and 4097-element cases. A same-process release microbenchmark used `2^17` extension/base pairs for 64 rounds in single-striped-striped-single order. Frontier single-accumulator times were `7.483208,7.465583 ms`; four-lane times were `8.898250,8.896291 ms`. The striped kernel was `19.05%` slower by mean and lost both pairings. | **Reject and revert before full proving.** Eight live low/high accumulator chains plus loop unrolling and final merges increase register pressure/code cost more than they hide scalar multiply latency on the M1. Do not test two/eight lanes without disassembly or counters showing spills can be avoided; the promoted single-chain delayed reducer remains best. |
| 2026-08-11 | Start decoding the longest remaining embedded circuit (`light_tx`) on a dedicated large-stack thread before joining the pre-circuit loader/prover, then consume that result in the existing remaining-circuit loader | Existing startup trace put pre load at `132.802 ms`, pre proof from `134.638` to `728.725 ms`, and the remaining four loads from `134.404` to `824.824 ms`, exposing a `96.099 ms` load tail. The ignored loader harness measured cold sequential pre/heavy-tx/heavy-chain/light-tx/light-chain at `94.9/346.5/84.7/367.2/83.6 ms`; all-five overlap was `633.1 ms`, sequential `976.8 ms`, rebuild `1.1 s`, warm `437.1 ms`. Same release binary SHA-256 `a5d5c466acc0f0861e71b0c0d0f944edbed9065a19d724a50105bdb709c530a7`, control `LIGHTER_EARLY_LIGHT_TX_LOAD=0`, fixed `B-C-C-B`: control `27.30,27.63 s`; candidate `27.71,27.97 s`. Candidate mean/median `27.840 s` versus control `27.465 s`, a `1.365%` runtime regression, and both pairings lost. | **Reject and revert without confirmation or submission.** Starting another heavyweight decode at process launch competes with the higher-priority pre load/proof and lengthens the critical path despite reducing the nominal remaining-loader tail. Do not start all four remaining decodes early; revisit startup only with phase-aware scheduling or a decode path proven not to contend for the shared Metal/CPU/memory resources. |
| 2026-08-11 | Suppress recursive destruction of finished circuit/proof input graphs after the final proof is owned, letting the worker's existing post-flush `_exit(2)` reclaim them wholesale | Existing trace: `prove_block_after_pre` ended at `34,215,961.875 us`, while its outer `block_pipeline` ended at `34,259,107.541 us`, exposing `43.146 ms` of return-time teardown immediately before serialization. Same release binary SHA-256 `2cf8789508fcaee07de9a14dfd8fe8aa61af1d81d26bd06b73ddfcd3b38695f1`, `B-C-C-B / C-B-B-C`, with B setting `LIGHTER_DROP_FINISHED_PROVER_STATE=1`: control `27.71,27.07,26.71,26.53 s`; candidate `26.29,27.04,27.09,26.71 s`. Candidate mean `26.7825` vs `27.005 s` (`0.82%` lower), median `26.875` vs `26.890 s` (`0.056%` lower), pairwise 2-2. Cargo check passed; trusted verification 1/1, public smoke `28.500265542 s` provisional only. Candidate checkpoint `d1d839a`; official submission `7244133b-9f3a-4072-b84d-89993282260e` (Yukon commit `23f3ca0`) scored `25.8191460929401 tx/s` against `29.8785698468374`, a `-4.0594237538973 tx/s` delta. | **Officially rejected and reverted.** The score landed in a broad low-service band and therefore does not resolve the predicted `~0.26%` mechanism, but the predeclared rule required an official frontier improvement. Retain the trace and safety evidence; do not keep or stack the deletion after its terminal rejection. |
| 2026-08-11 | Search four consecutive FRI proof-of-work witnesses per Rayon item with the existing AArch64 Poseidon2 x4 permutation, instead of one scalar permutation per item | Diagnostic profile: 106 PoW spans totaled `1.990932 s`, mean `18.782 ms`, max `298.942 ms`. Focused four-state differential passed 1/1. A 100,000-group microbenchmark measured four scalar permutations at `369.468 ms` versus x4 at `313.932 ms` (`1.177x`). Same release binary SHA-256 `b3b87b7811df5b306b9a11c4eabedc2eda3cbb16030b89faf1a9421e9855724a`, order `B-C-C-B / C-B-B-C`, with B setting `PLONKY2_FRI_POW_BATCH_WIDTH=1`: control `27.33,26.28,26.95,27.64 s`; candidate `27.08,27.98,26.96,26.41 s`. Candidate mean `27.1075` vs `27.050 s` (`0.21%` slower), median `27.020` vs `27.140 s` (`0.44%` nominally faster), pairwise signs 2-2. | **Reject and revert without submission.** The faster primitive is overlapped or offset by coarser `find_any` work; mixed aggregates and split signs show no critical-path gain. Do not try width two without new critical-path evidence. |
| 2026-08-11 | Force every FRI query round to be an independently stealable Rayon unit with `with_max_len(1)`, reducing long query-phase waits while several proofs share the global pool | The first eight-run test with the now-rejected pool candidate present measured `2.28%` lower mean runtime and 3-1 pairwise signs. After restoring promotion-137 best-fit, isolated binary SHA-256 `e391abdded403ddbb0246b70848dbac74252fc8f1351265e699cfdc77c4248ff` ran `B-C-C-B / C-B-B-C`: control `26.90,26.98,28.07,25.98 s`; candidate `27.32,26.39,26.31,27.46 s`. Candidate mean `26.870` vs `26.9825 s` (`0.42%` lower), median `26.855` vs `26.940 s` (`0.32%` lower), pairwise signs 2-2 and reverse-confirmation signs 1-1. Earlier trusted verification passed 1/1; source checkpoint `530fafd`. | **Reject and revert without submission.** The isolated effect is noise-sized and crossed the predeclared reverse-confirmation rule; the earlier larger signal depended on an officially rejected stack. Continue with query data-layout or copy removal rather than global iterator grain. |
| 2026-08-11 | Give each FRI query Rayon task a minimum grain of four queries, reducing per-proof query-task fan-out while several transaction/chain proofs already compete in the global pool | A diagnostic-only outer span measured 106 `build_query_proofs` phases totaling `1.627600 s`, mean `15.355 ms`, median `1.035 ms`, and max `232.077 ms`; recurring light transaction/chain contexts contained `186–232 ms` outliers, confirming a material tail. Same release binary SHA-256 `5616edc764fb6dc29ac26e92fd2a120096638182812b71c9872648ffb7a6e1c1`, screen `B-C-C-B`, with B setting `PLONKY2_FRI_QUERY_MIN_LEN=1`: control `30.10,29.41 s`, candidate-default-four `33.21,31.74 s`. Candidate mean/median `32.475 s` vs `29.755 s`, a `9.14%` runtime regression; both pairings lost. | **Reject and revert without confirmation.** Fine-grained within-proof query parallelism is valuable even under multi-proof concurrency; the observed tail is not fixed by coarsening every proof's tasks. Continue only with tree-major sibling materialization or copy/allocation removal, not a global minimum grain. |
| 2026-08-11 | Serialize the final proof into one pre-sized contiguous buffer instead of the existing 2 MiB `BufWriter` | Reused three diagnostic public-fixture traces from the column-pool campaign. `serialize_and_flush_proof` was `468.458`, `374.083`, and `357.250` microseconds, while corresponding process spans were `31.157`, `30.046`, and `33.030` seconds. The entire output path is only about `0.001–0.002%` of process time, including flush and close. | **Reject before implementation.** Even deleting the whole measured span cannot move official throughput measurably; proof encoding/serialization remains an uncrowded category because the current writer path is already below the noise floor. |
| 2026-08-11 | Preserve exact Metal column-store buffer sizes below 256 MiB, but retain best-fit for large/startup/final requests, so a 20 MiB borrower cannot cannibalize a recurring 64 MiB buffer and force later page faults | Diagnostic best-fit: 281 requests, 255 hits, 26 misses, 36 oversized loans, 10,716,446,720 miss-request bytes, nine 544 MiB misses. Pure exact reuse reduced 544 MiB misses to six but forced terminal 256/320 MiB allocations and was rejected. Hybrid: 26 misses, 9,133,096,960 miss-request bytes, six 544 MiB misses, no terminal 256/320 MiB misses. Release SHA-256 `20a1e68f5a22c7057d32395e795a91854bf88d23b3a60ac641c3f0d2cc52e4e9`; same-binary `B-C-C-B / C-B-B-C`, with B restoring best-fit by environment: control `35.65,29.96,35.95,34.77` s, candidate `32.77,30.73,34.83,35.03` s. Candidate median `33.80` vs `35.21` s (`4.00%` lower runtime); mean `33.34` vs `34.0825` s (`2.18%` lower); pairwise signs 2-2. Workspace check and trusted verification 1/1 passed. Candidate commit `b727fb0`; official submission `1d69b9c2-8554-47af-a40c-fce715581b57` (Yukon commit `c9f47d6`) scored `29.4067300861804 tx/s` against `29.8785698468374`, a `-0.4718397606570 tx/s` delta. | **Officially rejected and reverted.** The M1 structural counters and favorable aggregates did not transfer into a frontier gain on the M4 Pro; retain the evidence, but restore best-fit and do not stack this policy into later candidates. |
| 2026-08-11 | Restrict Rayon to the M1 Max's eight performance cores, or nine workers, instead of the default ten logical cores to reduce efficiency-core scheduling interference with Metal | Environment-only same-binary test using release SHA-256 `5589f6b9cd716d9f88a8a6a9a9cd7b44f4df44325d6490e1d8978f01d518d495`, with `LIGHTER_BUILD_BLOCK_CIRCUIT=1` forced in every arm so the rejected embedded-final candidate's extra blob/code was inert and identical. Eight-thread `D-C-C-D`: candidate `28.46, 26.74` s versus default `27.09, 27.22` s; medians `27.60` versus `27.155` s (`+1.64%` runtime). Nine-thread screen plus reverse confirmation: candidate `28.03, 26.26, 27.30, 26.95` s versus default `27.22, 27.61, 26.33, 27.24` s. Nine-thread median `27.125` s versus `27.230` s was a noise-sized `0.39%` nominal gain, while means were `27.135` versus `27.100` s (`0.13%` slower) and pairwise signs split 2-2. | **Reject; no source change.** Removing both efficiency-core workers clearly lengthened the critical path, while removing one produced no repeatable effect. Rayon worker count alone does not bind work to Apple core classes, and the official M4 Pro has a different core topology. Revisit only with QoS/affinity control plus phase counters, not a global thread-count heuristic. |
| 2026-08-11 | Build and compact-embed the fixed final `BlockCircuit` in the untimed Cargo job, then decode it on the existing block lane instead of constructing it during proving | Reconstructed on promotion #137 as a five-file same-binary candidate with original runtime construction selectable by `LIGHTER_BUILD_BLOCK_CIRCUIT=1`. Release SHA-256 `5589f6b9cd716d9f88a8a6a9a9cd7b44f4df44325d6490e1d8978f01d518d495`; generated `block.embed` was 4.46 MiB. The six-circuit identity oracle passed after comparing commitment semantics independently of CPU-vs-Metal backing representation: targets, coefficient polynomials, parameters, leaf counts, full caps, verifier data, sigma data, generator streams, common data, and every remaining prover-only field matched. Trusted public smoke verification passed 1/1 at `29.0962` s. Same-binary out-of-sandbox Metal `B-C-C-B`: runtime-builder control `26.40, 26.85` s; embedded candidate `27.23, 28.24` s. Candidate median `27.735` s versus `26.625` s is `+1.110` s or `+4.17%` runtime (`-4.00%` throughput). | **Reject and revert.** The earlier CPU-fallback experiment (`184.99` s runtime build vs `158.24` s embedded, `14.46%` faster) does not transfer to the current Metal frontier. Compact decoding reconstructs the final constants/sigmas commitment while the transaction pipeline is active, adding work/queue contention that the existing concurrent builder hides more effectively. Reconsider only if the final blob can retain or reconstruct proof data without entering the serialized Metal commitment queue, or if decode is delayed to an actually idle phase. |
| 2026-08-11 | Reduce jemalloc arenas to eight (`MALLOC_CONF=narenas:8`) to match the M1 Max performance-core count and improve large-extent reuse | Environment-only same-binary test using release SHA-256 `ce5db429ceeb2c9632d0cce575c3acaa93fa1a382b6228a89a200ed1c10a4062`, with the Metal threadgroup cap fixed at the frontier's 128 in every run. Fixed screen `D-C-C-D` followed by reverse confirmation `C-D-D-C`; direct `/usr/bin/time -p` samples were candidate `30.01, 28.88, 31.50, 30.76` s and default `30.34, 30.28, 30.21, 29.32` s. Candidate median `30.385` s versus default `30.245` s is `+0.46%` runtime; means were `30.2875` s versus `30.0375` s (`+0.83%`). The initial two-sample screen appeared `2.85%` faster, but both confirmation pairings favored default and erased the signal. | **Reject; no source change.** Eight arenas did not improve repeatably and slightly regressed both aggregate statistics. Do not encode an M1 core-count heuristic for the M4 Pro; arena count should be revisited only with allocator statistics showing fragmentation or lock contention, or as an official-host matrix rather than whole-proof local drift. |
| 2026-08-11 | Tune the blanket 1-D Metal dispatch threadgroup cap from the frontier's 128 threads to 64 or 256 for Apple GPU occupancy | Same release binary SHA-256 `ce5db429ceeb2c9632d0cce575c3acaa93fa1a382b6228a89a200ed1c10a4062`, clean-environment Metal screening in fixed `128-256-64-64-256-128` order followed by alternating 64/128 confirmation. Direct `/usr/bin/time -p` samples: cap 64 `25.90, 27.97, 32.30, 35.43` s; cap 128 `27.16, 31.74, 29.69, 30.75` s; cap 256 screening `27.45, 28.40` s. The 64 median was `30.135` s versus `30.220` s for 128 (a noise-sized `0.28%` nominal throughput gain), but its mean was `30.400` s versus `29.835` s (`1.89%` slower), and it won the first two pairings but lost the two confirmation pairings by `2.61` and `4.68` s. | **Reject and revert.** The sign followed run order and machine state rather than threadgroup width; neither 64 nor 256 showed a repeatable advantage over 128. A blanket cap is too coarse because quotient, NTT-adjacent hashing, leaves, parents, and absorb kernels have different register/occupancy behavior. Revisit only with per-kernel caps and GPU execution-time counters on the M4 Pro, not whole-proof wall time on a thermally drifting M1 Max. |
| 2026-08-11 | Return `BaseSumGate<2>(63)` to CPU only when an existing CPU-only exponentiation or interleave gate already makes its 64 constraint rows and wires free | Same release binary SHA-256 `f198f3c23e3c282e5af82557e4e710ef508722958bf2c44f58ebb8063ff424bd`, clean-environment Metal runs with the frontier restored by `PLONKY2_METAL_BINARY_BASE_SUM=1`. After discarding two samples whose wrapper timing was incomplete, direct `/usr/bin/time -p` samples were candidate `38.26, 37.31, 34.48` s and frontier control `39.28, 37.02, 33.61` s. Candidate median `37.31` s versus control `37.02` s is `+0.29` s or `+0.78%` runtime (`-0.78%` throughput); means were `36.6833` s versus `36.6367` s (`+0.13%` runtime). A routing diagnostic confirmed BaseSum63 stayed on Metal in degree-14 pre/chain circuits and moved to CPU only in the degree-16 transaction/final shapes, without increasing their existing CPU row maxima. | **Reject and revert.** The structurally selective policy removed candidate 1's clear chain-circuit penalty, but it did not produce a repeatable gain: one pair favored the candidate and two favored the frontier, with both aggregate statistics slightly negative. The extra CPU work merely traded against Metal tail work on the local M1 Max. Reconsider only with M4-specific counters or a broader scheduling change that overlaps this CPU work rather than serializing it. |
| 2026-08-10 | Return `BaseSumGate<2>(63)` from the shared Metal Range/U32 quotient command to its packed CPU evaluator | Same release binary SHA-256 `97401a67a84343d3a716bfc32d1bea2d5405055879ac90f3403f13650302c342`, clean-environment Metal runs in predeclared `C-B-B-C-C-B` order: candidate `29.4954, 28.2697, 27.7576` s; frontier control `28.3905, 27.1270, 26.8526` s. Candidate median `28.2697` s versus control `27.1270` s: `+1.1427` s or `+4.21%` runtime (`-4.04%` throughput). The default candidate also produced one final recursive proof accepted by the pinned trusted verifier; its `29.9498` s public-synthetic smoke time is noncompetitive and is not compared with official TPS. | **Reject and revert.** Diagnostics confirmed the gate moved as intended, but chain proofs raised the CPU constraint-row ceiling from 26 to 64; that extra dense evaluation outweighed the shorter Metal tail on the local M1 Max. Do not submit this shape alone. |
| 2026-08-11 | Replace the streamed Metal Merkle builder's per-eight-column heap `Vec<&mut [F]>` with fixed stack slice descriptors, after auditing wire-IFFT, coefficient, LDE, and leaf ownership for a larger copy | #138 already moves non-routed witness columns, fuses the routed borrow-to-IFFT gather, writes scaled coefficients directly into the final shared Metal store, and hashes that same buffer without upload or transpose. A 106-proof trace had 53 degree-14, 52 degree-16, and one degree-18 proof; aggregate wire LDE traffic was about `36.79 GiB`, but streaming activated only for the final proof's 172 columns: 22 heap allocations and 2,752 descriptor bytes, zero field payload. Both Metal candidate/control tree differentials matched the classic complete digest tree and cap. Release SHA-256 `a7a83b077d2ca435f68e47da64e7ed917ccfa7716b743262c44f0cf3c67686af`; trusted verification 4/4. Fixed `B-C-C-B`: control `36.380620708,31.302977125 s` (mean `33.841798917`); stack `40.062462042,33.563807417 s` (mean `36.813134729`), `+8.780076%` runtime / `-8.071401%` throughput-equivalent; both pairings lost. Frontier refresh found newer promotion `5a25029` at `30.3111567697189 tx/s`, whose ramp-depth-two and saturation evidence was not present in the #138 source tested here. Yukon note `180fdf87-e9aa-45f8-a9bc-00d99f175fda`. | **Reject and revert without submission.** This is descriptor bookkeeping, not the payload copy implied by the aggregate spans. Preserve #138's boundary; do not micro-tune slice vectors or buffer handles. Rebase future payload-scale fill/absorb fusion or range-U32 LDE-read work on the new promoted frontier and retain exact root/proof controls. |

## Update checklist

1. Read the live promoted count and record from `https://lighter.fast/`.
2. Run `yukon submissions --all` and inspect each new promoted note.
3. Append one row per promotion; label marker-only or behavior-identical changes
   as `redraw` and add no inherited category hits.
4. Recompute category counts and shares using the new promoted total.
5. Update the snapshot metadata and commit this file with the candidate.
