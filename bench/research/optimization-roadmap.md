# Lighter prover optimization roadmap

This file tracks the ordered optimization campaign separately from
`promoted-options.md`, which is the dataset of public promoted submissions.
Update this roadmap after every meaningful implementation, benchmark,
verification, rejection, submission, or change in priority.

Last updated: 2026-08-13 12:41, America/Chicago

Current public frontier: promotion #145, `7477de7` / submission `a955674`,
`30.7380852237325 tx/s`. Local experiment rows retain their historical frontier
references so submitted and rejected deltas remain reproducible.

Live refresh: `origin/master` is still exactly `7477de7`; no post-#145
promotion has landed. Eight submissions were validating at the 12:41 refresh.
The strongest newly terminal near-frontier candidates were direct transcript
allocation deletion (`887e5b0`, `30.4508863812249 tx/s`) and exclusive-phase
spin completion waits (`bb20477`, `30.4206092764959 tx/s`; `1b8ad6d`,
`30.3974360350032 tx/s`); all were rejected.

## Status legend

- **Queued** — not implemented or measured yet.
- **Drafted** — source exists, but required compilation or measurement is incomplete.
- **Testing** — an isolated correctness or performance experiment is active.
- **Partial** — useful evidence exists, but the current frontier/M4 transfer case is unproven.
- **Rejected** — measured evidence crossed the rejection threshold and the change was reverted.
- **Operational** — an experiment-selection or submission policy rather than a code change.

## Ordered ideas

| # | Optimization angle | Status | Current evidence and update | Next action |
|---:|---|---|---|---|
| 1 | Binary BaseSum-63 CPU fallback | **Rejected** | Same release binary, Metal-backed `C-B-B-C-C-B` runs: candidate median `28.2697 s`, frontier-control median `27.1270 s`; candidate was `+4.21%` slower (`-4.04%` throughput). Trusted verification passed, but chain proofs raised the CPU constraint-row ceiling from 26 to 64. | Do not submit or retest the blanket fallback; retain only as evidence for selective admission. |
| 2 | Gate-by-gate CPU/Metal admission matrix | **Rejected (BaseSum-63)** | Selective row-free admission routed BaseSum-63 to CPU only in degree-16 transaction/final shapes and retained Metal in degree-14 chain shapes. Same-binary candidate median `37.31 s` versus frontier `37.02 s` (`+0.78%` runtime); mean was `+0.13%`. | Do not submit the selective BaseSum policy. Profile a different gate family only when counters show a queue-tail contribution exceeding its CPU survivor cost. |
| 3 | Metal threadgroup-size sweep by kernel | **Partial; blanket sweep rejected** | Same-binary blanket 1-D caps were non-repeatable: cap-64 median `30.135 s` versus cap-128 `30.220 s`, but means favored 128 by `1.89%` and confirmation reversed the early signal; cap-256 also showed no win. | Add per-kernel GPU execution-time counters and test quotient, leaf, parent, and absorb kernels separately; do not repeat a whole-pipeline cap sweep. |
| 4 | Column-store pool capacity and reuse policy | **Officially rejected; reverted** | Best-fit made 36 oversized loans totaling 1.80 GiB of excess mapping. The hybrid exact-below-256-MiB policy cut miss-request bytes from 10.72 GB to 9.13 GB and measured `-4.00%` local median runtime, but pairwise signs split 2-2. Candidate `b727fb0`, official submission `1d69b9c2-8554-47af-a40c-fce715581b57` (Yukon commit `c9f47d6`) scored `29.4067300861804 tx/s` against `29.8785698468374`, a `-0.4718397606570 tx/s` delta. | Keep the diagnostic evidence only. Best-fit is restored; do not stack this M1-positive/M4-negative policy into later candidates. |
| 5 | Earlier buffer retirement at proof-stage boundaries | **Officially rejected; reverted** | Commit `82635d8` combined exact reuse below 256 MiB with ownership retirement of the three per-proof initial FRI trees. Local `B-C-C-B` improved mean runtime `2.94%`, won both pairings, and passed trusted verification 5/5. Official submission `9345ccf4-7264-4f4b-ab72-9bcb6755ec1e` (Yukon `89366c0`) scored `29.3561409924708 tx/s` versus frontier `29.9399105848455`, a `-0.5837695923747 tx/s` delta. Source was reverted in `029af57`. | Do not keep or redraw this combined stack. The fast-band M4 result rejects the M1 signal; exact-bin reuse and the earlier lifetime boundary were toggled together, so neither component receives isolated official credit. Revisit only with a different ownership/reuse interaction and new M4-side evidence. |
| 6 | FRI delayed-reduction tile tuning | **Rejected (coefficient-slot block); other parameters untested** | Same-binary symmetric sweep `2048-4096-1024-1024-4096-2048` passed trusted verification 6/6. Means were 2,048 `37.418893958 s`, 4,096 `42.996962541 s` (`+14.9071%`), and 1,024 `46.379565646 s` (`+23.9469%`). Large endpoint drift makes the exact deltas noisy, but neither alternative had a positive aggregate signal; release SHA-256 `3f12ff6e3da450d8cd85de2048667f153bccabf477b54dec1df6c0b40727b650`. | Retain 2,048 and do not repeat a blind M1 power-of-two sweep. Other reduction intervals or shape-aware tiles require per-reduction counters or direct M4 access before implementation. |
| 7 | Adaptive proof-pipeline window | **Static neighbors rejected; adaptive policy queued** | A diagnostic trace recorded 47 `tx_proof_window_join` waits totaling `18.464573 s`, but clean symmetric `6-7-5-5-7-6` evidence retained six. Depth 5 mean `31.945402937 s` versus six `31.029418500 s` (`+2.951987%`, both pairings lost); depth 7 mean `32.195706020 s` (`+3.758651%`, pairings split). Release SHA-256 `3d7036f089a134e0f78ce304e4e3554a82c7f0e7d3c5fa9a039809e698bf1f6e`; trusted verification 6/6. Yukon note `f36ea396-3c8e-495a-8eb0-943964650119`. | Keep six. A useful adaptive policy must distinguish heavy-path contention/drain phases from light-only throughput using portable queue state; do not test another static depth or memory-capacity heuristic. |
| 8 | Rayon pool and Apple performance-core scheduling | **Rejected (global thread count)** | `RAYON_NUM_THREADS=8` regressed median runtime by `1.64%`; nine threads had a `0.39%` nominal median gain but a `0.13%` mean regression and split pairwise signs 2-2. | Do not hard-code a worker count. Revisit only with phase-specific QoS/affinity plus counters, especially on the actual M4 Pro topology. |
| 9 | Allocator configuration | **Partial; eight arenas rejected** | `MALLOC_CONF=narenas:8` initially appeared `2.85%` faster but failed reverse confirmation; four-sample median was `+0.46%` runtime and mean `+0.83%`. Default decay remains inherited and correct. | Do not encode the M1 core-count arena heuristic. Measure fragmentation/lock statistics before testing background thread, tcache, or large-allocation settings. |
| 10 | Final-block circuit embedding on the current frontier | **Rejected on Metal** | Six-circuit semantic identity and trusted verification passed, but current-frontier Metal `B-C-C-B` measured embedded median `27.735 s` versus runtime build `26.625 s`, a `+4.17%` regression. The older CPU-only `14.46%` win did not transfer. | Do not submit the compact final blob. Revisit only if decode can avoid reconstructing constants/sigmas on the active serialized Metal queue. |
| 11 | Generator data-layout locality | **Rejected (tested variants)** | Post-seed watcher-template same-binary `B-C-C-B` measured baseline median `183.36 s` versus candidate `186.58 s` (`+1.76%` runtime); trusted verification passed 1/1. Dense scatter was officially rejected at `-33.46%`, and batched watcher accounting was `3.639%` slower. | Require new phase evidence before another generator-layout rewrite; do not recombine these rejected variants. |
| 12 | Merkle/FRI query zero-copy paths | **Rejected (tested grain variants)** | `with_min_len(4)` regressed `9.14%`. Max grain one initially measured `2.28%` faster while the now-rejected pool policy was present, but the isolated promotion-137 `B-C-C-B / C-B-B-C` rerun reduced that to `0.42%` by mean and `0.32%` by median, with pairwise signs split 2-2. Exact isolated SHA-256 `e391abdded403ddbb0246b70848dbac74252fc8f1351265e699cfdc77c4248ff`; source checkpoint `530fafd`. | Max grain one crossed its reverse-confirmation rejection rule and was reverted. Continue with query-data layout/copy elimination, not global iterator grain. |
| 13 | Proof serialization into one pre-sized output buffer | **Rejected by profile ceiling** | Three diagnostic public workers measured the complete `serialize_and_flush_proof` span at `0.357`, `0.374`, and `0.468 ms` inside `30.05–33.03 s` processes: roughly `0.001–0.002%` of runtime. The existing 2 MiB `BufWriter` already makes this path immaterial. | Do not implement a single-buffer serializer unless the official fixture or proof format changes enough to make output exceed `0.1%` of runtime. |
| 14 | M4-aware static admission policy | **Queued / dependent** | Blanket BaseSum-63 regressed `4.21%` and structurally selective BaseSum-63 regressed `0.78%` by median. No per-shape policy has positive repeatable evidence yet. | Accumulate counter-backed results on the actual M4 Pro before hard-coding any architecture-specific admission decision. |
| 15 | Leaderboard redraw for statistically credible candidates | **Operational** | No new redraw has been submitted from this campaign. The policy is to redraw only verified candidates with repeatable local evidence or an already-strong executable, never to treat marker-only promotions as algorithmic evidence. | Apply only after a candidate survives correctness, local A/B, and frontier refresh checks. |
| 16 | Four-state Poseidon2 FRI proof-of-work search | **Rejected** | Profiled PoW spans totaled `1.991 s`. Existing x4 was `1.177x` faster than four scalar permutations and passed a four-state differential, but same-binary eight-run end-to-end means regressed `0.21%`, medians nominally improved `0.44%`, and pairwise signs split 2-2. | Reverted and not submitted. Revisit PoW only if a dependency/profile change puts it on the proof-window critical path. |
| 17 | Skip finished prover-state teardown before `_exit` | **Officially rejected; reverted** | A trace exposed `43.146 ms` of return-time teardown. Same-binary mean improved `0.82%`, median `0.056%`, pairwise 2-2, and trusted verification passed 1/1. Candidate `d1d839a`; submission `7244133b-9f3a-4072-b84d-89993282260e` (Yukon `23f3ca0`) scored `25.8191460929401 tx/s` against `29.8785698468374`, a `-4.0594237538973 tx/s` delta. | The official run landed in a broad low-service band and is inconclusive about a `~0.26%` effect, but it crossed the predeclared failure rule. Source is reverted; retain only the trace/safety evidence. |
| 18 | Start `light_tx` embedded decode before the pre proof | **Rejected; reverted** | Startup tracing showed a `96.099 ms` remaining-loader tail and `light_tx` was the longest remaining cold decode at `367.2 ms`. A dedicated early loader nevertheless lost both `B-C-C-B` pairings: control `27.30,27.63 s`, candidate `27.71,27.97 s`; candidate mean/median regressed `1.365%`. Same binary SHA-256 `a5d5c466acc0f0861e71b0c0d0f944edbed9065a19d724a50105bdb709c530a7`. | Do not start the other remaining decodes at launch. The extra decode competes with the pre load/proof; revisit only with phase-aware scheduling or a noncontending codec/data path. |
| 19 | Dependency-striped delayed-reduction opening dot product | **Rejected; reverted** | Four 160-bit lanes per quadratic limb preserved field values in focused boundary tests, but the production-degree release kernel screen was decisively negative: frontier single `7.483208,7.465583 ms` versus striped `8.898250,8.896291 ms`, `+19.05%` mean runtime, both pairings lost. | Keep the promoted single accumulator. Do not try two/eight lanes without assembly or hardware-counter evidence that register pressure/spills can be avoided. |
| 20 | Retire ephemeral commitment coefficients before FRI | **Rejected; reverted** | Wire/Z/quotient coefficients were dropped after reduced-polynomial construction while Merkle trees/caps and reusable constants remained intact. Trusted verification passed 5/5, but same-binary `B-C-C-B` control mean `30.117623313 s` beat candidate `30.495082562 s`; candidate was `1.2533%` slower and lost both pairings. | Do not eagerly free the inner vectors on the FRI critical path. Explore ownership transfer or pool reuse that avoids serialized destructor/deallocation work. |
| 21 | Bitmap-aware witness initialization and adjacent zero-fill audit | **Promoted externally in #138; inspect before extending** | The #138 public note reports deleting dense `PartitionWitness::new` zero-fill and making the sole unguarded materialization path bitmap-select zero for unset slots, avoiding roughly 71 MB of stores per transaction proof and 285 MB for the final proof. The promotion arrived inside a larger scheduling/Metal/memory/FFT stack, so the isolated official contribution is unknown. | Sync and inspect the exact #138 guards first. Do not duplicate its change; profile other large initialized witness/generator buffers and require a complete reader audit before testing any uninitialized allocation. |
| 22 | Reduce pipeline witness-materialization Rayon fanout | **Rejected; reverted** | A diagnostic trace found 106 calls and 617.2M output cells; same-shape light transaction materialization averaged `164.7 ms` versus `16.2 ms` for heavy, with a `1.286 s` tail. Nevertheless clean same-binary `16-8-8-16` measured control mean `29.177586500 s` versus eight-task mean `29.509238020 s` (`+1.1367%`), pairings split 1-1, trusted verification 4/4. | Keep 16. The tail is pool competition, not simple task fanout overhead. Do not test four/one blindly; require a dedicated-pool, inline-prefix, or portable proof-criticality mechanism with new evidence. |
| 23 | Late sparse-zero witness materialization | **Rejected; reverted** | Fill only unset representative slots at `full_witness` entry, then gather without per-cell bitmap selection. Same-binary `B-C-C-B` passed trusted verification 4/4, but control mean `30.023932187 s` beat candidate `30.746826896 s`; candidate regressed `2.407728%` and lost both pairings. Release SHA-256 `4de3e0720759bc60cbd5e5641bfdd6759e3959045935dfe076527e09fd6c6595`. | Preserve #138's uninitialized allocation plus bitmap-guarded gather. The compact bitmap work is cheaper than serially dirtying millions of unset slots; do not test wider or parallel zero fills without a profile showing bitmap selection on the critical path. |
| 24 | Unchecked full-witness gather indexing | **Rejected; reverted** | Use validated representative-map and disjoint-segment invariants to remove map, bitmap, values, and destination bounds checks while retaining identical bitmap zero selection. Focused proof differential and trusted verification passed, but same-binary control mean `29.925012479 s` beat candidate `30.110599667 s`; candidate regressed `0.620174%` and lost both pairings. Release SHA-256 `75aa7b436e8d9062e234112332787a52291a59c53168256d3516b2e918e8693d`. | Keep the safe running-index loop. Do not introduce unsafe indexing based on source syntax; require disassembly or counters proving a residual check, and preserve frontier address arithmetic if revisited. |
| 25 | Demand-zero witness allocation variants | **Rejected; isolated and reverted** | The first zeroed+unguarded screen had a nominal `0.888451%` mean win but split pairings. A three-mode diagnostic then separated allocation and gather: one trace per mode measured aggregate gather work at #138 `3.469742 s`, zeroed+guarded `2.857983 s`, and zeroed+unguarded `4.222018 s`, with a `494.060 ms` unguarded tail. The refined zeroed-allocation-only candidate retained the exact bitmap guard and safely specialized only `GoldilocksField`; release SHA-256 `ca4e6719c9da2115ea4a7249e40589568a723ba5b07b66f0caa62aaecaab9e59`. Protected `B-C-C-B` control mean was `29.589916146 s`, candidate mean `30.195013229 s`: `+2.044944%` runtime, `-2.003964%` throughput-equivalent, both pairings lost, trusted verification 5/5 including the cold gate. Yukon note `7f0ca8ad-82af-4649-8abf-946b3e33d33b`. | Close this line and preserve #138's uninitialized values plus bitmap-guarded reads. The isolated negative shows zeroed allocation/first-touch cost exceeds any benefit; do not retest demand-zero on M1, and require direct M4 VM counters before reopening it. |
| 26 | Four-cell full-witness gather unroll | **Rejected by pairing rule; reverted** | Four explicit independent map/bitmap/value chains replaced the inner iterator only in the candidate arm; the baseline arm retained #138. Release SHA-256 `d42a8d12dbb6aaa3cfcf755a0ce1b126b7b36998f2625a97344d5ae4fa96f924`; Cargo check, two focused tests, and trusted verification 5/5 passed. `B-C-C-B` control mean `30.990668917 s`, candidate mean `30.584303021 s`, nominal `-1.311252%` runtime / `+1.328675%` throughput, but pairings split `-5.241951%` and `+2.922815%` as control endpoints drifted by 2.303 s. Yukon note `95ec0035-2ae1-4ea0-b028-ebcfee26fa57`. | Keep the compiler-generated iterator loop. Do not test two/eight-wide source unrolls without release disassembly or Apple counters showing a load-dependency stall and a stable protected benchmark signal. |
| 27 | Wire-commit/Merkle streamed slice-descriptor allocation | **Rejected; reverted** | Ownership tracing found no remaining payload upload, transpose, or intermediate LDE allocation on #138's successful Metal path. The only removable boundary allocation was 22 short-lived `Vec<&mut [F]>` instances covering 172 fat pointers (2,752 bytes) in the final proof's three streamed commitments, versus about `36.79 GiB` of aggregate wire-LDE payload traffic. Same-binary stack/control release SHA-256 `a7a83b077d2ca435f68e47da64e7ed917ccfa7716b743262c44f0cf3c67686af`; complete Metal tree/cap differentials passed in both modes; trusted verifier passed 4/4. `B-C-C-B` control mean `33.841798917 s`, candidate `36.813134729 s`: `+8.780076%` runtime, both pairings lost. Yukon note `180fdf87-e9aa-45f8-a9bc-00d99f175fda`. During the terminal refresh, `5a25029` became the official frontier at `30.3111567697189 tx/s` with a ramp-depth-two gain and measured steady GPU/buffer saturation. | Keep #138's boundary code and do not optimize descriptor or Objective-C handle clones as payload copies. The next boundary candidate must delete a measured field-element pass or fuse fill/absorb work, and it must first rebase on the newer official frontier rather than stacking onto this older source. |
| 28 | Range-U32 quotient wire-LDE threadgroup tiling | **Rejected by pairing rule; reverted** | On #139, a separate tiled kernel staged 16 quotient rows by up to 136 wire columns (17,408 bytes) with 32 loader lanes and used the exact scalar kernel as `PLONKY2_RANGE_U32_TILE=0` control. Cargo check and five focused Metal/CPU quotient differentials passed. Release SHA-256 `351e738e5fd2fe356924ed8e613c9b3a0682b107d419e0bca5246fea7672bfcb`; trusted verification 5/5. Fixed `B-C-C-B`: control `47.109057000,44.662899292 s` (mean `45.885978146`); tiled `46.737068125,46.334087584 s` (mean `46.535577855`), `+1.415682%` runtime / `-1.395921%` throughput. Pairings split `-0.789633%` then `+3.741782%`. The stale embedded metallib forced both arms through identical cold source compilation, inflating absolute times but not the within-binary selector delta. | Revert without submission. Do not sweep tile sizes on M1; a future M4-only revisit needs direct kernel counters and a SIMD-group-native layout. Continue with exact final-block buffer prewarm, then wire-commit fill/absorb fusion. |
| 29 | Exact final-block first-submit Metal buffer prewarm | **Rejected; reverted** | On #139, the existing utility thread additionally allocated and fully touched the final wires commitment's exact 272 MiB coefficient input and ~128 MiB Merkle output before publishing one-shot exact-size stashes; `PLONKY2_FINAL_SET_PREWARM=0` retained the promoted column-store-only prewarm. Exact-size guard test and Cargo check passed. Release SHA-256 `124031eb1719fb1094994e6a9a93f83b217b99d0a4780a47bce3767b3d18d98c`; trusted verification 5/5. Fixed `B-C-C-B`: control `36.780856334,40.475705500 s` (mean `38.628280917`); candidate `42.431655375,39.706284625 s` (mean `41.068970000`), `+6.318399%` runtime / `-5.942903%` throughput. Pairings split `+15.363424%/-1.900945%`. | Revert without submission. Moving another 400 MiB of page faults under the critical light pipeline creates more unified-memory/VM contention than it removes from the final tail. Do not prewarm more buffers or sweep page stride/QoS without M4 counters; proceed to fill/absorb work elimination. |
| 30 | Paired wire-fill / fused Metal sponge absorbs | **Rejected by pairing rule; reverted** | On #139, candidate mode filled up to 16 retained LDE columns per Rayon batch and kept the 12-lane sponge state live across two rate-eight permutations; `PLONKY2_WIRE_ABSORB_FUSION=0` restored the promoted eight-column path. Candidate/control 17-column Metal full-tree/cap/path differentials passed. Release SHA-256 `7fc550d1bab4b3aa1263803b9d7ded8cd145b88370da9ec850607fda3241067b`; trusted verification 5/5. `B-C-C-B`: control `35.424763792,29.965593667 s` (mean `32.695178730`); candidate `30.936116792,30.167929875 s` (mean `30.552023334`), nominal `-6.554958%` runtime / `+7.014774%` throughput, but pairings split `-12.670930%/+0.675228%` under 5.459 s control drift. Refresh then found marker-only promotion #140 at `30.4408404924460 tx/s`. | Revert without submission. The warm reverse pairing rejects the candidate despite its favorable aggregate; keep eight-column fill/absorb overlap. Revisit only with direct M4 command/state-traffic counters or a fusion scheme that preserves first-absorb latency. |
| 31 | Parallel production permutation Z chains | **Rejected by pairing rule; reverted** | On #140-equivalent source, one `plonky2_maybe_rayon::join` computed the two independent post-inversion Z accumulations; `PLONKY2_PARALLEL_Z_CHAINS=0` preserved serial construction. Cargo check and the production-shape fixed-mask symbolic/raw-limb test passed in both modes. Release SHA-256 `a149fbe83bce6d851d464d3af2add242187de1d358d478ebaa03a09ac81605b2`; trusted verification 5/5. `B-C-C-B`: control `30.587060542,31.190676041 s` (mean `30.888868292`); candidate `31.385469750,29.611569541 s` (mean `30.498519646`), nominal `-1.263719%` runtime / `+1.279894%` throughput, but pairings split `+2.610284%/-5.062752%`. | Revert without submission. A 10–18 ms mechanism cannot explain 0.6–1.8 s arm drift, and the first pairing formally loses. Keep serial chains; revisit only with a dedicated-pool/phase mechanism or direct microspan counters showing this tail is both critical and uncontended. |
| 32 | SIMD-native partial-prefix Range/U32 tile | **Rejected by pairing rule; reverted** | A production access-set census found no useful compact gate partition: degree-16/18 records span roughly 114–136 wire columns and every advertised family is evaluated on every quotient-coset row. The revised kernel staged 32 rows by the first 120 columns (30 KiB), assigned all 32 SIMD lanes one evaluated row, and direct-loaded only the tail. Cargo check, six focused candidate/control quotient differentials, the combined proof check, and trusted verification 5/5 passed; release SHA-256 `8d6dbb73aa8b02b235726d43a3ec33910b4b080b14087fa6b163ae7e88b4a1c6`. `B-C-C-B` control mean `38.731961083 s` versus candidate `38.765839666 s`: candidate `+0.087469%` runtime / `-0.087393%` throughput, with pairings `-1.188258%/+1.373715%`. The terminal refresh found marker-only #141 at `30.4758937588950 tx/s`. | Revert without submission. The SIMD-native layout removed the predecessor's half-idle lanes but did not beat direct device reads on M1; do not sweep prefix/row sizes locally. Revisit only with direct M4 occupancy/cache/queue-tail counters. |
| 33 | First-absorb-preserving tail-pair fusion | **Rejected after first pairing; reverted** | Group zero retained the exact eight-column fill and original Metal kernel, while only later groups were filled/absorbed in pairs so two permutations kept their intermediate state private. Cargo check and candidate/control 17-column full-tree/cap/path differentials passed; release SHA-256 `11a2c41ef4c2fd6030225ae4a40975f5ed7317ec708111e00aad8dbe64788306`; trusted verification 3/3. The first declared B-C pairing was scalar `30.949020167 s` versus candidate `31.455793292 s`: candidate `+1.637445%` runtime / `-1.611065%` throughput. | Revert without completing C2/B2 or submitting: one lost pairing makes the two-pair success rule impossible. Delaying the first absorb was not the cause of the prior split; close fill/absorb fusion on M1 unless direct M4 counters justify a structurally different design. |
| 34 | Phase-aware chain quotient submission burst | **Rejected by pairing rule; reverted** | A 10,196-event Metal trace found the light transaction queue ending at `22.715 s` while the chain had completed only 19/49 steps, leaving 30 proofs and `3.649 s` of serial tail. The candidate used the existing `SPINE_BACKLOG >= 3` signal to prioritize one adjacent three-command chain quotient burst; it changed no GPU work, concurrency, or buffers. Cargo check and candidate-default trusted verification passed; release SHA-256 `fc9df0d96b9d3e11c5c43e5b87df9c74cee8f874fa26bd227aadfdc9d87cf945`. Direct `B-C-C-B`: control mean `30.030 s`, candidate `30.235 s` (`+0.682651%` runtime), pairings `-7.656613%/+9.100033%`. | Revert without submission. The host-side admission lock was neutral-negative and unstable. Keep the measured tail, but advance predecessor-independent chain work or remove an exact dependency instead of adding another global queue-order lock. |
| 35 | Exclusive degree-14 wire GPU-NTT commitment | **Rejected after first pairing; reverted** | The boundary audit confirmed that non-ZK wire LDEs already materialize directly in final shared Metal storage, so the candidate instead admitted the existing fused NTT-to-Merkle backend only for 136-wire degree-14 commitments during a proven exclusive phase with zero Metal jobs in flight. Cargo check passed; clean release SHA-256 `e03fe21e91099f0c4423aa6fc4224216eed6d91b22228be04cdd66e3afab85d2`; trusted verification 3/3. The candidate gate was `29.402302958 s`; first protected B-C pairing was control `28.537014959 s` versus candidate `29.432943167 s`, a `+3.13953%` runtime regression. | Revert without C2/B2 or submission. Even on an idle queue, the current GPU NTT is slower than parallel CPU LDE into final shared storage for this shape; do not broaden admission. Revisit only with a materially different NTT kernel or direct M4 kernel evidence. |
| 36 | Parallel exclusive chain predecessor-feed generators | **Rejected by pairing rule; reverted** | The trace proved all 30 late successors had already prepared independent witness work and waited `2.898–9.940 s`; only the post-predecessor feed remained, totaling `234.631 ms` (`7.821 ms/step`). The candidate enabled the existing threshold-64 parallel worklist only during the proven exclusive drain. Cargo check passed; release SHA-256 `d2ecb6e7dc381d4e3f08dea145c5d75160ab20bd044aff051efa0571b3d157ae`; trusted verification 5/5. Protected `B-C-C-B`: control `32.425009041,29.618962542 s`, candidate `29.765828500,29.694780875 s`; pairings `-8.20102%/+0.255979%`. | Revert without submission. The favorable mean is dominated by a 2.806 s control drift far above the 234.6 ms ceiling; the quiet reverse pairing is negative. Do not implement a ready queue because current successor threads already provide it; revisit feed work only with a dedicated executor or larger dependency. |
| 37 | `RandomAccessGate<bits=6>` stack scratch | **Rejected by focused ceiling; reverted** | The only CPU-surviving random-access shape needs 1,024 temporary field slots, so the old 256-slot stack fast path fell back to a `Vec<MaybeUninit<F>>` on roughly 852,000 32-row calls per worker. Expanding the stack scratch to 8 KiB and using a test-only forced-heap same-binary control measured `140 ns/row` versus `142 ns/row` (nominal `1.0143x`) with overlapping arms. The actual saving is about `64 ns/call`, `~0.055 s` aggregate CPU, or only `~7 ms / 0.02%` optimistic wall time. | Revert before full proving or submission. `MaybeUninit` construction performs no 8 KiB fill; only the jemalloc tcache allocation/free pair was removed. Keep the heap fallback and target arithmetic/materialization inside the bits=6 fold only if a microbenchmark exposes a materially larger ceiling. |
| 38 | Packed/NEON `RandomAccessGate<bits=6>` selector fold | **Rejected by pairing rule; reverted** | A shape-specialized 64-column by 32-row fold used preferred AArch64 packing, selector-major traversal, contiguous packed loads, and fused `x.multiply_accumulate(b, y - x)`. Two rotated same-binary confirmations measured scalar `130 ns/row` versus fused `107 ns/row`, with disjoint arms; packing without fusion was `113–114 ns/row`. The `23 ns/row` saving projected `~0.628 s` aggregate CPU / `~78 ms` optimistic wall. All four RandomAccess tests passed, including raw noncanonical-word equality. Release worker SHA-256 `7de015f21f64a08b1eff76c9632887741222c7bbbf0d228134d0e346059befb0`; protected verification 5/5. `B-C-C-B`: control `29.598131208,30.218835167 s` (mean `29.908483188`); candidate `28.582630792,30.876317000 s` (mean `29.729473896`), nominal `-0.598523%` runtime / `+0.602127%` throughput, but pairings split `-3.430961%/+2.175735%` and candidate endpoints drifted `2.294 s`, 29x the mechanism ceiling. | Revert without submission in `dec9697`. The direct evaluator gain is real but too small for this M1 protected harness to attribute end-to-end; do not submit the favorable mean. Preserve commit `941f7e1` as the recoverable implementation for a future M4 station-level or lower-noise measurement. |

| 39 | Chain-tail Metal station hold / buffer-set widening | **Partial; widening rejected by profile** | The production trace still leaves 30 serial chain proofs and `3.649 s` after the light transaction queue drains, but the corrected whole-worker sample attributes only 26 of 409,039 samples to Metal buffer-set acquisition (`0.006%` of all samples, effectively `0%` of wait). The main thread's `pthread_join` accounts for `46.2%` of wait and parked Rayon workers another `38.5%`; those are not evidence that a second Metal set would release CPU work. Two exclusive spin-wait submissions were also rejected at `30.4206` and `30.3974 tx/s`. | Do not widen the buffer set or optimize completion wakeups on this evidence. Revisit chain staging/readback only if direct phase spans—not thread wait counts—show a repeatable critical slice above `~0.2 s/worker`. |
| 40 | Heterogeneous CPU/GPU LDE split | **Partial; overlap trace next** | FFT/NTT is 70,145 samples (`41.5%` of compute), but straight GPU reassignment is closed: at production shapes CPU versus isolated GPU-NTT medians were `34.33` vs `~43.16 ms` for degree 14 and `153.94` vs `~168.62 ms` for degree 16, confirming the earlier `+3.14%` end-to-end loss. The degree-16 rates are nevertheless within `~10%`; a modeled per-column split is `1.45x` if the measured `49%` GPU-free fraction actually overlaps CPU LDE. No prior experiment split one commitment across both devices. | Correlate CPU LDE phase intervals with existing GPU command-buffer timestamps before implementation. If meaningful same-phase GPU idle exists, preserve the first eight-column CPU→absorb boundary and prototype a tail-column split; otherwise close heterogeneous routing. Do not substitute aggregate GPU idle or a whole-commitment GPU route for this measurement. |

## Related completed experiments

These experiments overlap the roadmap but are not substitutes for the exact
queued ideas above.

| Experiment | Result | Decision |
|---|---|---|
| CPU-survivor static alpha reduction | Candidate approximately `0.387%` slower | Reverted |
| FRI initial-oracle leaf gathering | Candidate approximately `0.218%` slower | Reverted |
| Batched initial generator-watcher accounting | Focused timing approximately `3.639%` slower | Reverted |
| Dense transaction-value scatter | Official score `28.6955` TPS versus `29.7608` frontier (`-33.46%` challenge delta) | Rejected |
| Pre-resolved transaction-input seed metadata | Correctness checks passed, but no qualifying private-active timing evidence | Reverted / not submitted |
| Blanket BaseSum-63 CPU fallback | `4.21%` slower by Metal median | Reverted |
| Selective row-free BaseSum-63 CPU fallback | `0.78%` slower by Metal median; `0.13%` slower by mean | Reverted |
| Blanket Metal 1-D threadgroup cap sweep | Cap 64/128 result changed sign; cap 256 showed no win | Reverted |
| Jemalloc `narenas:8` | `0.46%` slower by median; `0.83%` slower by mean | No source change |
| Rayon 8/9 workers | Eight regressed `1.64%`; nine was aggregate noise | No source change |
| Final-block compact embedding | Older CPU-only `14.46%` win became `4.17%` slower on current-frontier Metal | Reverted; do not submit |
| Hybrid exact-bin Metal column-store pool | `4.00%` lower local median, but official `29.4067300861804` vs `29.8785698468374 tx/s` (`-0.47184`) | Officially rejected; reverted |
| Pre-sized proof serialization buffer | Full serialize-and-flush span only `0.357–0.468 ms` (`~0.001–0.002%` of worker time) | Rejected before implementation |
| FRI query Rayon minimum grain of four | Control `30.10,29.41 s`; candidate `33.21,31.74 s`, `9.14%` slower by mean; both pairings lost | Reverted without confirmation |
| FRI query Rayon maximum grain of one | Isolated control `26.90,26.98,28.07,25.98 s`; candidate `27.32,26.39,26.31,27.46 s`; candidate only `0.42%` faster by mean and `0.32%` by median, pairwise 2-2 | Reverted; not submitted |
| Four-state Poseidon2 PoW batching | Control mean/median `27.050/27.140 s`; candidate `27.1075/27.020 s`; mean `0.21%` slower, median `0.44%` nominally faster, pairwise 2-2 | Reverted; not submitted |
| Finished prover-state destructor elision | Local trace-derived fixed cost `43.146 ms`; official `25.8191460929401` vs `29.8785698468374 tx/s` (`-4.0594237538973`) | Officially rejected; reverted |
| Early `light_tx` embedded decode | Control `27.30,27.63 s`; candidate `27.71,27.97 s`; candidate `1.365%` slower and lost both pairings | Reverted without confirmation |
| Four-lane opening dot-product accumulators | Single `7.483208,7.465583 ms`; striped `8.898250,8.896291 ms`; striped `19.05%` slower and lost both pairings | Reverted before full proving |
| Early retirement of per-proof coefficient vectors | Control `29.915660042,30.319586584 s`; candidate `30.211574167,30.778590958 s`; candidate `1.2533%` slower, trusted verification 5/5 | Reverted without confirmation |
| FRI coefficient-slot block sweep | 2,048 mean `37.418893958 s`; 4,096 mean `42.996962541 s` (`+14.9071%`); 1,024 mean `46.379565646 s` (`+23.9469%`); trusted verification 6/6 | Retain 2,048; selector reverted without confirmation |
| Exact-size Metal reuse plus FRI initial-tree retirement | Local candidate `2.94%` lower by mean and both pairings won, but official `29.3561409924708` versus `29.9399105848455 tx/s` (`-0.5837695923747`) | Officially rejected; reverted in `029af57` |
| Degree-14/16 witness materialization fanout eight | Control mean `29.177586500 s`; candidate mean `29.509238020 s` (`+1.1367%`); pairings 1-1; trusted verification 4/4 | Reverted without confirmation |
| Late sparse-zero witness materialization | Control mean `30.023932187 s`; candidate mean `30.746826896 s` (`+2.4077%`); both pairings lost; trusted verification 4/4 | Reverted without confirmation or submission |
| Unchecked full-witness gather indexing | Control mean `29.925012479 s`; candidate mean `30.110599667 s` (`+0.6202%`); both pairings lost; trusted verification 4/4 | Reverted without confirmation or submission |
| Demand-zero witness allocation variants | Initial zeroed+unguarded mean improved `0.8885%` but split pairings; isolated zeroed+guarded follow-up regressed `2.044944%`, lost both pairings, and passed trusted verification 5/5 including the cold gate | Fully rejected and reverted; retain #138 uninitialized allocation plus bitmap guard |
| Four-cell full-witness gather unroll | Candidate mean improved `1.311252%`, but pairings split `-5.241951%` and `+2.922815%` under a 2.303 s control-endpoint swing; trusted verification 5/5 | Reverted by pairing rule; not submitted |
| Light proof-window 5/6/7 sweep | Six mean `31.029418500 s`; five `31.945402937 s` (`+2.951987%`, both pairings lost); seven `32.195706020 s` (`+3.758651%`, pairings split); trusted verification 6/6 | Retain six; no source change or submission |
| Streamed Metal slice-descriptor stack storage | Removed 22 heap allocations / 2,752 descriptor bytes but zero field payload; candidate mean `36.813134729 s` versus control `33.841798917 s` (`+8.780076%`), both pairings lost, trusted verification 4/4 | Reverted without submission; pursue payload-scale passes only |
| Exclusive degree-14 wire GPU-NTT commitment | Candidate gate `29.402302958 s`; first protected pairing control `28.537014959 s` versus candidate `29.432943167 s` (`+3.13953%` runtime); trusted verification 3/3 | Reverted after first pairing; current CPU-LDE plus GPU-hash split retained |
| Parallel exclusive chain predecessor-feed generators | Candidate samples `29.765828500,29.694780875 s`; controls `32.425009041,29.618962542 s`; pairings split `-8.20102%/+0.255979%`, trusted verification 5/5 | Reverted by pairing rule; implicit ready queue retained |
| `RandomAccessGate<bits=6>` stack scratch | Same-binary focused medians `140 ns/row` stack versus `142 ns/row` heap (`1.0143x`), overlapping arms; projected whole-worker wall ceiling about `0.02%` | Reverted before full proving; allocation-only hypothesis closed |

## Experiment update checklist

For every idea tested:

1. Record the base commit, candidate commit or exact dirty diff, release-binary SHA-256, hardware, and environment.
2. Verify the intended path with diagnostics before trusting timing results.
3. Run proof compatibility through the pinned trusted verifier when possible.
4. Use a predeclared alternating same-binary order and report every sample, median, mean, and throughput-equivalent delta.
5. Mark the idea **Rejected** and revert it when it crosses its failure threshold; do not keep ambiguous code in the frontier.
6. Refresh `promoted-options.md` and the live submission queue before committing or submitting a surviving candidate.
7. Update this roadmap and `experiment-results.tsv` after every keep/reject decision, trusted verification result, submission creation, and final promoted/rejected/failed submission outcome.
8. For an official submission, record its full ID, candidate commit, frontier score at submission time, official score/delta, status, and note reference in both logs before considering the experiment closed.
